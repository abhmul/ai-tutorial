---
aliases:
title: 2026-06-04
tags:
  - daily-note
  - ai-generated
---
# 2026-06-04
[[2026-06-03]]<- [[2026-06-04]] -> [[2026-06-05]]

## Session Notes

### Decouple Skill Setup

**Goal:** Separate reusable skill behavior from project- and harness-specific setup instructions for the orchestration and web-discovery examples.

**Changes:**
- Added `SETUP.md` files for `skills/orchestration/` and `skills/web-discovery/`.
- Revised both `SKILL.md` files to focus on runtime behavior and point setup questions to `SETUP.md`.
- Removed the old `skills/web-discovery/references/setup.md` file.
- Updated `README.md`, `skills/README.md`, and `recommendations.md` to refer to `SETUP.md` and clarify that Pi means the `pi` coding harness, not Raspberry Pi hardware.
- Added a copy-paste prompt in `README.md` for agent-assisted skill installation with human review.

**Decisions:**
- Kept Pi/tmux worker details in orchestration setup rather than the orchestration skill itself.
- Kept Ketch and equivalent web backend details in web-discovery setup rather than the web-discovery skill itself.
- Left untracked `.agents/` and `.claude/` files unstaged because they are outside the current task slice.

**Validation:**
- Ran `git diff --check`; no whitespace errors.

**Open Items:**
- [ ] Consider adding shorter per-skill install prompts in `skills/README.md` if users need more targeted setup examples.

**Self-Reflection:**
I initially left more implementation detail in the active skill files than ideal; the improved split is cleaner. In future skill work, start by defining the boundary between runtime instructions and setup instructions before editing content.
